doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
        link(rel="stylesheet", href="https://unpkg.com/mvp.css") 
    body 
        header 
            h1  Noom
             
        main
            div#welcome
                form
                    input#input_nickname(placeholder="input nickname", type="text")
                    input#input_roomname(placeholder="room name", require  ,type="text")
                    button Enter Room  
                h4  Open Rooms: 
                ul 
            div#room
                span 
                h3 
                ul
                form#name
                    input(placeholder="input change nickname", type="text")
                    button Change Name  
                form#msg
                    input(placeholder="write message", require  ,type="text")
                    button Send 
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")